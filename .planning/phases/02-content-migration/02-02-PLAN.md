---
phase: 02-content-migration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - site/scripts/scrape.mjs
  - site/scripts/parsers/people.mjs
  - site/scripts/parsers/events.mjs
  - site/src/content/people/
  - site/src/content/events/
autonomous: true
requirements:
  - MIG-02
  - MIG-03
must_haves:
  truths:
    - "Visitor at /people sees all 19 real team members grouped as Leadership / Affiliates / Staff"
    - "Each person MDX has name, role (enum), title, and optional photo path /images/people/{slug}.{ext}"
    - "Visitor at /events sees all scraped events with title, speaker, date, abstract"
    - "All migrated events have upcoming: false (historic from /talksevents/); future flagging deferred to editorial"
    - "pnpm build succeeds with full people + events corpus"
  artifacts:
    - path: "site/scripts/parsers/people.mjs"
      provides: "Divi .et_pb_team_member to person frontmatter + bio body parser"
      min_lines: 60
    - path: "site/scripts/parsers/events.mjs"
      provides: "Divi+Framer events-row parser with speaker/date/abstract extraction"
      min_lines: 60
    - path: "site/src/content/people/"
      provides: "19 person MDX files grouped by role"
      contains: "role: leadership"
    - path: "site/src/content/events/"
      provides: "30+ event MDX files (past events, upcoming: false)"
      contains: "upcoming: false"
  key_links:
    - from: "site/scripts/parsers/people.mjs"
      to: "section headings Leadership/Affiliates/Staffs/Alumni"
      via: "iterate sections, map heading text to role enum"
      pattern: "Leadership|Affiliates|Staffs|Alumni"
    - from: "site/scripts/parsers/events.mjs"
      to: "ordinal-strip regex for 'May 3th 2024' typo"
      via: "(\\d+)(st|nd|rd|th) replaced before parseDate"
      pattern: "st\\|nd\\|rd\\|th"
    - from: "site/scripts/scrape.mjs"
      to: "parsers/{people,events}.mjs"
      via: "--people and --events flags"
      pattern: "--people|--events"
---

<objective>
Extend the scraper with people and events parsers, then run them against the live site to migrate all 19 team members and 30-41 events.

Purpose: MIG-02 (events) + MIG-03 (people) — populate the `/people` and `/events` index pages with real content. Both depend on Plan 02-01's scraper foundation (orchestrator, libs, schema edits, deps).
Output: Two new parser files in `site/scripts/parsers/`, `--people` and `--events` flags wired into the orchestrator, and ~19 person + ~30-41 event MDX files in their respective content collections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-migration/02-CONTEXT.md
@.planning/phases/02-content-migration/02-RESEARCH.md
@.planning/phases/02-content-migration/02-01-SUMMARY.md
@site/src/content.config.ts
@site/src/content/people/bill-howe.mdx
@site/src/content/events/ai-normal-technology.mdx

<interfaces>
people schema (post-Plan-02-01 edits):

  name: z.string()
  role: z.enum(['leadership', 'affiliate', 'staff', 'alumni'])
  title: z.string()
  department: z.string().optional()
  email: z.string().email().optional()
  x: z.string().optional()
  linkedin: z.string().optional()
  website: z.string().optional()
  photo: z.string().optional()
  researchAreas: z.array(z.string()).default([])
  order: z.number().default(99)
  linkedPublications: z.array(reference('publications')).default([])
  linkedTalks: z.array(reference('events')).default([])

events schema:

  title: z.string()
  speaker: z.string()
  speakerBio: z.string().optional()
  speakerPhoto: z.string().optional()
  date: z.coerce.date()
  time: z.string().optional()
  location: z.string().default('Online')
  abstract: z.string().optional()
  recording: z.string().optional()
  slides: z.string().optional()
  upcoming: z.boolean().default(false)

Sample MDX target formats (inspect via read_first):
- site/src/content/people/bill-howe.mdx — person frontmatter (name, role, title, order, researchAreas) followed by bio prose body
- site/src/content/events/ai-normal-technology.mdx — event frontmatter only (title, speaker, date, upcoming: false), no body

Shared lib imports (from Plan 02-01):
- writeEntry, cleanFrontmatter from ../lib/mdx.mjs
- makeSlug, resetSlugCache from ../lib/slugify.mjs
- parseDate from ../lib/dates.mjs
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement people parser and run migration</name>
  <files>site/scripts/parsers/people.mjs, site/scripts/scrape.mjs, site/src/content/people/</files>
  <read_first>
    - site/src/content.config.ts (people schema — role enum, field types)
    - site/src/content/people/bill-howe.mdx (sample: frontmatter + bio body prose)
    - site/scripts/lib/mdx.mjs (writeEntry signature)
    - site/scripts/lib/slugify.mjs (makeSlug, resetSlugCache)
    - site/scripts/scrape.mjs (orchestrator — to add --people flag dispatch)
    - .planning/phases/02-content-migration/02-RESEARCH.md (Pattern 4 people block shape, Pattern 6 bio body, Code Examples "Parsing people", Pitfalls #7 asset host, #9 researchAreas)
  </read_first>
  <action>
    Implement the people parser against Divi .et_pb_team_member blocks with section-heading role mapping.

    **A. Clean slate.** Before running, the parser deletes existing src/content/people/*.mdx (3 handcrafted files: bill-howe, chirag-shah, tanu-mitra). Per CONTEXT.md D-02, `git diff` preserves review discretion post-scrape.

    **B. Implement site/scripts/parsers/people.mjs using the section-iteration approach (reliable):**

    Structure the parser as: walk all `.et_pb_section` elements in document order. For each section, find the first role heading descendant (h1/h2/h3 with text matching Leadership/Affiliates/Staffs/Staff/Alumni). If found, set `currentRole`. Then for each `.et_pb_team_member` in that section, emit a person with `currentRole`.

    ```
    import { rm } from 'node:fs/promises';
    import { writeEntry } from '../lib/mdx.mjs';
    import { makeSlug, resetSlugCache } from '../lib/slugify.mjs';

    const OUT_DIR = 'src/content/people';

    const ROLE_MAP = {
      Leadership: 'leadership',
      Affiliates: 'affiliate',
      Staffs: 'staff',
      Staff: 'staff',
      Alumni: 'alumni',
    };

    function cleanBioText($bioEl, $) {
      const clone = $bioEl.clone();
      clone.find('*').each((_, el) => {
        const $el = $(el);
        $el.removeAttr('style').removeAttr('class').removeAttr('color').removeAttr('face');
      });
      const paras = clone.find('p').map((_, p) => $(p).text().replace(/\s+/g, ' ').trim()).get().filter(Boolean);
      if (paras.length > 0) return paras.join('\n\n');
      return clone.text().replace(/\s+/g, ' ').trim();
    }

    function extractTwitterHandle(href) {
      if (!href) return undefined;
      const m = href.match(/(?:twitter\.com|x\.com)\/(?:@)?([\w]+)/i);
      return m ? '@' + m[1] : undefined;
    }

    function findRoleForSection($section, $) {
      const headings = $section.find('h1, h2, h3').toArray();
      for (const h of headings) {
        const label = $(h).text().trim();
        if (ROLE_MAP[label]) return ROLE_MAP[label];
      }
      return null;
    }

    export async function parsePeople($) {
      resetSlugCache();
      await rm(OUT_DIR, { recursive: true, force: true });

      const people = [];
      let currentRole = null;
      let orderCounter = 1;

      $('.et_pb_section').each((_, section) => {
        const $section = $(section);
        const sectionRole = findRoleForSection($section, $);
        if (sectionRole) currentRole = sectionRole;
        if (!currentRole) return;

        $section.find('.et_pb_team_member').each((_, tm) => {
          const $tm = $(tm);
          const name = $tm.find('.et_pb_module_header').text().trim();
          if (!name) return;

          const title = $tm.find('.et_pb_member_position').text().trim() || 'Member';
          const bio = cleanBioText($tm.find('.et_pb_team_member_description'), $);
          const email = $tm.find('a[href^="mailto:"]').attr('href')?.replace(/^mailto:/, '');

          const xHref = $tm.find('.et_pb_twitter_icon').parent('a').attr('href')
            || $tm.find('a[href*="twitter.com"], a[href*="x.com"]').attr('href');
          const linkedin = $tm.find('.et_pb_linkedin_icon').parent('a').attr('href')
            || $tm.find('a[href*="linkedin.com"]').attr('href');
          const website = $tm.find('a[href^="http"]')
            .not('[href*="twitter"]').not('[href*="x.com"]').not('[href*="linkedin"]')
            .not('[href^="mailto:"]').first().attr('href');

          const photoUrl = $tm.closest('.et_pb_row').find('.et_pb_image img').first().attr('src')
            || $tm.find('img').first().attr('src');

          people.push({
            name,
            role: currentRole,
            title,
            email,
            x: extractTwitterHandle(xHref),
            linkedin,
            website,
            _photoUrl: photoUrl,
            bio,
            order: orderCounter++,
          });
        });
      });

      // Deduplicate by name (in case a person appears in two sections).
      const byName = new Map();
      for (const p of people) if (!byName.has(p.name)) byName.set(p.name, p);
      const dedup = Array.from(byName.values());

      let count = 0;
      for (const p of dedup) {
        let photo;
        if (p._photoUrl) {
          const urlPath = new URL(p._photoUrl, 'https://sites.uw.edu').pathname;
          const ext = (urlPath.match(/\.(jpg|jpeg|png|webp|gif)$/i)?.[0] ?? '.jpg').toLowerCase();
          photo = `/images/people/${makeSlug(p.name)}${ext}`;
        }
        const frontmatter = {
          name: p.name,
          role: p.role,
          title: p.title,
          ...(p.email && { email: p.email }),
          ...(p.x && { x: p.x }),
          ...(p.linkedin && { linkedin: p.linkedin }),
          ...(p.website && { website: p.website }),
          ...(photo && { photo }),
          researchAreas: [],
          order: p.order,
        };
        const filename = makeSlug(p.name) + '.mdx';
        await writeEntry({ dir: OUT_DIR, filename, frontmatter, body: p.bio ?? '' });
        count++;
      }
      return count;
    }
    ```

    **C. Wire --people flag into orchestrator.** Edit `site/scripts/scrape.mjs` to import `parsePeople` and dispatch:

    ```
    import { parsePeople } from './parsers/people.mjs';
    // inside main():
    if (runAll || args.has('--people')) {
      console.log('Scraping people...');
      const $ = await fetchPage('https://raise.uw.edu/people/');
      const count = await parsePeople($);
      console.log(`  Wrote ${count} people`);
    }
    ```

    **D. Run and verify:**

    ```
    cd site && pnpm scrape -- --people
    ```

    Expected: ~19 people written. Each MDX has `name:`, `role:` (one of leadership/affiliate/staff), `title:`, `order:`, and an optional `photo: /images/people/{slug}.{ext}` pointing to a file that does NOT exist yet (Plan 02-03 downloads it). Schema allows this — `photo` is bare `z.string().optional()`.

    Run `pnpm build`. MUST succeed. If ZodError fires on `role`, the heuristic miscategorized — inspect and fix section iteration.

    **E. Role distribution sanity check:** `grep -l "role: leadership" src/content/people/*.mdx | wc -l` should be ~3; `affiliate` ~14; `staff` ~2.
  </action>
  <verify>
    <automated>cd site && pnpm scrape -- --people && test $(ls src/content/people/*.mdx | wc -l) -ge 17 && pnpm build && test $(grep -l "role: leadership" src/content/people/*.mdx | wc -l) -ge 1 && test $(grep -l "role: affiliate" src/content/people/*.mdx | wc -l) -ge 5</automated>
  </verify>
  <acceptance_criteria>
    - site/scripts/parsers/people.mjs exports parsePeople (grep `export async function parsePeople`)
    - site/scripts/parsers/people.mjs selects on `.et_pb_team_member` (stable token)
    - site/scripts/parsers/people.mjs contains ROLE_MAP with all four enum values: `leadership`, `affiliate`, `staff`, `alumni`
    - site/scripts/parsers/people.mjs contains Staffs AND Staff keys in ROLE_MAP (tolerates either spelling)
    - site/scripts/parsers/people.mjs uses spread-conditional `...(val && { field: val })` pattern
    - site/scripts/scrape.mjs contains `--people` flag dispatch AND imports parsePeople
    - `ls site/src/content/people/*.mdx | wc -l` is between 17 and 22 (target 19)
    - At least one file contains `role: leadership`
    - At least five files contain `role: affiliate`
    - Every file contains `name:` `role:` `title:` `order:` at minimum
    - `cd site && pnpm build` exits 0 after people scrape
    - No `.mdx` frontmatter contains `role: undefined` or `role:` on a line with no value
  </acceptance_criteria>
  <done>
    ~19 person MDX files exist in site/src/content/people/ with correct role mapping (leadership/affiliate/staff), order values, bio prose bodies, and photo paths pointing to /images/people/{slug}.{ext} (files to be downloaded in Plan 02-03). Build passes with people corpus. The 3 handcrafted samples (bill-howe, chirag-shah, tanu-mitra) regenerated; git diff review captures any handcrafted bio enrichment worth preserving.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement events parser and run migration</name>
  <files>site/scripts/parsers/events.mjs, site/scripts/scrape.mjs, site/src/content/events/</files>
  <read_first>
    - site/src/content.config.ts (events schema — all fields optional except title/speaker/date)
    - site/src/content/events/ai-normal-technology.mdx (sample frontmatter-only format)
    - site/scripts/lib/dates.mjs (parseDate supports ordinal-strip via regex)
    - site/scripts/lib/mdx.mjs (writeEntry signature)
    - site/scripts/lib/slugify.mjs (makeSlug, resetSlugCache)
    - site/scripts/scrape.mjs (orchestrator — to add --events flag dispatch)
    - .planning/phases/02-content-migration/02-RESEARCH.md (Pattern 5 event block shape, Pitfall #2 date formats, Pitfall #6 Framer-wrapped events, Open Question #3 event count)
  </read_first>
  <action>
    Implement the events parser. This is the most fragile parser (Divi + Framer mixed layout) — be defensive.

    **A. Clean slate.** Parser deletes existing src/content/events/*.mdx (2 handcrafted: ai-normal-technology, faeze-llm-adaptation). Same overwrite policy as publications/people.

    **B. Implement site/scripts/parsers/events.mjs:**

    ```
    import { rm } from 'node:fs/promises';
    import { writeEntry } from '../lib/mdx.mjs';
    import { makeSlug, resetSlugCache } from '../lib/slugify.mjs';
    import { parseDate } from '../lib/dates.mjs';

    const OUT_DIR = 'src/content/events';

    // Regex for event date+time strings found in /talksevents/:
    //   "Jun 6th 2025, 9:00 – 10:00AM PT"
    //   "May 3th 2024, 10:00AM PT"  (note typo: "3th" not "3rd")
    // Strategy: strip ordinal suffix first (handled in parseDate), then split on first comma.
    function parseDateTimeBlob(raw) {
      if (!raw) return { date: undefined, time: undefined };
      const cleaned = raw.trim();
      // Split on first comma: left = date, right = time
      const commaIdx = cleaned.indexOf(',');
      let datePart, timePart;
      if (commaIdx >= 0) {
        datePart = cleaned.slice(0, commaIdx).trim();
        timePart = cleaned.slice(commaIdx + 1).trim();
      } else {
        datePart = cleaned;
      }
      const date = parseDate(datePart);
      return { date, time: timePart };
    }

    // Extract speaker, speakerBio, abstract from the Framer-wrapped text block.
    // Source has markers: "Guest Speaker" ... "Abstract" ...
    function extractEventText(fullText) {
      const text = fullText.replace(/\s+/g, ' ').trim();
      const speakerMarker = text.search(/Guest Speaker/i);
      const abstractMarker = text.search(/Abstract/i);

      let speaker, speakerBio, abstract;
      if (speakerMarker >= 0 && abstractMarker > speakerMarker) {
        const between = text.slice(speakerMarker + 'Guest Speaker'.length, abstractMarker).trim();
        // First line is the speaker name; remainder is speakerBio
        const firstNewline = between.search(/\.\s/);  // first sentence end is often the name boundary
        if (firstNewline >= 0 && firstNewline < 120) {
          speaker = between.slice(0, firstNewline).replace(/^[:\-\s]+/, '').trim();
          speakerBio = between.slice(firstNewline + 1).trim();
        } else {
          speaker = between.split(/\s{2,}|\s(?=[A-Z][a-z]+ is )/)[0]?.trim() ?? between.slice(0, 80).trim();
          speakerBio = between.length > 80 ? between.slice(speaker.length).replace(/^[:\-\s]+/, '').trim() : undefined;
        }
      }
      if (abstractMarker >= 0) {
        abstract = text.slice(abstractMarker + 'Abstract'.length).replace(/^[:\-\s]+/, '').trim();
        // Trim trailing Zoom-join sentence if present
        abstract = abstract.replace(/\s*(Join Zoom Meeting.*|https?:\/\/\S*zoom\S*).*$/i, '').trim();
      }
      return { speaker, speakerBio, abstract };
    }

    export async function parseEvents($) {
      resetSlugCache();
      await rm(OUT_DIR, { recursive: true, force: true });

      const events = [];

      // Iterate .et_pb_row blocks (stable token); each row is a candidate event.
      $('.et_pb_row').each((_, row) => {
        const $row = $(row);

        // A row counts as an event if it has either:
        //   a Zoom link, or a "Guest Speaker" text marker, or an image title+date-looking text.
        const rowText = $row.text();
        const hasGuestSpeaker = /Guest Speaker/i.test(rowText);
        const hasZoom = $row.find('a[href*="zoom"]').length > 0;
        if (!hasGuestSpeaker && !hasZoom) return;

        // Title: prefer image title attr (often matches event title), else first Framer h1/h2.
        const titleFromImg = $row.find('img[title]').first().attr('title')?.trim();
        const titleFromHeading = $row.find('h1, h2, h3, h4').first().text().trim();
        const title = titleFromImg || titleFromHeading;
        if (!title) return;

        // Date+time: look for a text node matching the pattern "{Month} {day}(st|nd|rd|th) {year}, ..."
        const dateMatch = rowText.match(/(\w+ \d{1,2}(?:st|nd|rd|th)? \d{4}(?:, [^A-Z]*?(?:AM|PM)\s*PT)?)/i);
        const { date, time } = parseDateTimeBlob(dateMatch?.[1]);
        if (!date) return;  // omit events we can't date

        const { speaker, speakerBio, abstract } = extractEventText(rowText);

        // Recording / slides: look for non-Zoom external links (youtube, vimeo, slides.com, etc.)
        let recording, slides;
        $row.find('a[href]').each((_, a) => {
          const href = $(a).attr('href') ?? '';
          if (/youtube\.com|youtu\.be|vimeo\.com/i.test(href)) recording = recording ?? href;
          if (/slides\.com|slideshare|docs\.google\.com\/presentation/i.test(href)) slides = slides ?? href;
        });

        events.push({
          title,
          speaker: speaker || 'Guest Speaker',
          speakerBio,
          date,
          time,
          abstract,
          recording,
          slides,
          upcoming: false,  // All /talksevents/ entries are past events
        });
      });

      // Deduplicate by title (rows can double-count).
      const byTitle = new Map();
      for (const e of events) if (!byTitle.has(e.title)) byTitle.set(e.title, e);
      const dedup = Array.from(byTitle.values());

      let count = 0;
      for (const e of dedup) {
        const frontmatter = {
          title: e.title,
          speaker: e.speaker,
          ...(e.speakerBio && { speakerBio: e.speakerBio }),
          date: e.date,
          ...(e.time && { time: e.time }),
          ...(e.abstract && { abstract: e.abstract }),
          ...(e.recording && { recording: e.recording }),
          ...(e.slides && { slides: e.slides }),
          upcoming: false,
        };
        const filename = makeSlug(e.title) + '.mdx';
        await writeEntry({ dir: OUT_DIR, filename, frontmatter, body: '' });
        count++;
      }
      return count;
    }
    ```

    **C. Wire --events flag into orchestrator.** Edit site/scripts/scrape.mjs to import parseEvents and dispatch:

    ```
    import { parseEvents } from './parsers/events.mjs';
    // inside main():
    if (runAll || args.has('--events')) {
      console.log('Scraping events...');
      const $ = await fetchPage('https://raise.uw.edu/talksevents/');
      const count = await parseEvents($);
      console.log(`  Wrote ${count} events`);
    }
    ```

    **D. Run and verify:**

    ```
    cd site && pnpm scrape -- --events
    ```

    Expected: 25-41 events written (RESEARCH.md Open Question #3 — live count is authoritative, "50+" estimate in REQUIREMENTS was optimistic). Each MDX has `title:`, `speaker:`, `date:`, `upcoming: false`. Fields `speakerBio`, `time`, `abstract`, `recording`, `slides` present only when extractable.

    Run `pnpm build`. MUST succeed. If ZodError fires on `date`, inspect the offending file and tweak `parseDateTimeBlob` regex.

    **E. Quality spot-check:**

    ```
    cat site/src/content/events/$(ls site/src/content/events/ | head -1) | head -20
    ```

    First file should have sensible title, speaker, YYYY-MM-DD date, optional abstract ~1-3 sentences.

    **F. Final full build:**

    ```
    cd site && pnpm build && ls dist/ | grep -E "(events|people|publications)"
    ```

    Built site should now render all three index pages with real content.
  </action>
  <verify>
    <automated>cd site && pnpm scrape -- --events && test $(ls src/content/events/*.mdx | wc -l) -ge 20 && pnpm build && test $(grep -l "upcoming: false" src/content/events/*.mdx | wc -l) -ge 20</automated>
  </verify>
  <acceptance_criteria>
    - site/scripts/parsers/events.mjs exports parseEvents (grep `export async function parseEvents`)
    - site/scripts/parsers/events.mjs selects on `.et_pb_row` (stable token)
    - site/scripts/parsers/events.mjs contains `Guest Speaker` string literal AND `Abstract` string literal
    - site/scripts/parsers/events.mjs contains ordinal-tolerant regex: `st|nd|rd|th` appears in a regex
    - site/scripts/parsers/events.mjs always emits `upcoming: false` (grep `upcoming: false` in source)
    - site/scripts/scrape.mjs contains `--events` flag dispatch AND imports parseEvents
    - `ls site/src/content/events/*.mdx | wc -l` >= 20 (target 25-41)
    - Every event MDX has `title:`, `speaker:`, `date:`, `upcoming: false`
    - No event MDX has `date: 1970-01-01` (would indicate parseDate returned epoch)
    - `grep "upcoming: true" site/src/content/events/*.mdx` returns nothing (all are past)
    - `cd site && pnpm build` exits 0 after full scrape (publications + people + events)
  </acceptance_criteria>
  <done>
    20-41 event MDX files exist in site/src/content/events/ with title, speaker, date, upcoming: false, and optional speakerBio/abstract/recording/slides when extractable. Build passes with the full content corpus (publications + people + events). Handcrafted event samples replaced; git diff review captures any preservation decisions. `pnpm scrape` without flags runs all three parsers end-to-end.
  </done>
</task>

</tasks>

<verification>
Plan-level checks (run after both tasks):

1. People count and role distribution:
   ```
   ls site/src/content/people/*.mdx | wc -l          # >= 17 (target 19)
   grep -l "role: leadership" site/src/content/people/*.mdx | wc -l   # ~3
   grep -l "role: affiliate" site/src/content/people/*.mdx | wc -l    # ~14
   grep -l "role: staff" site/src/content/people/*.mdx | wc -l        # ~2
   ```

2. Events count and upcoming flag:
   ```
   ls site/src/content/events/*.mdx | wc -l         # >= 20
   grep -l "upcoming: false" site/src/content/events/*.mdx | wc -l   # equals total
   grep "upcoming: true" site/src/content/events/*.mdx               # empty (no matches)
   ```

3. Orchestrator flag routing:
   ```
   grep -E "(--people|--events)" site/scripts/scrape.mjs
   ```
   Both flags must be present with dispatch logic.

4. Full-site build passes:
   ```
   cd site && pnpm build
   ```
   Exits 0 with all three index pages rendered from real content.

5. Sample inspection (eyeball one person + one event):
   ```
   head -15 site/src/content/people/bill-howe.mdx 2>/dev/null || head -15 site/src/content/people/$(ls site/src/content/people/ | head -1)
   head -15 site/src/content/events/$(ls site/src/content/events/ | head -1)
   ```
</verification>

<success_criteria>
- MIG-02 satisfied: 20-41 events from raise.uw.edu exist as MDX in site/src/content/events/ with title, speaker, date, upcoming: false (historic), and optional speakerBio/abstract/recording/slides where extractable
- MIG-03 satisfied: ~19 people from raise.uw.edu exist as MDX in site/src/content/people/ with name, role enum, title, order, bio body, and photo path /images/people/{slug}.{ext} (files landed in Plan 02-03)
- Scraper orchestrator routes --people, --events, and --all flags correctly
- `cd site && pnpm build` passes with full content corpus (publications from 02-01 + people + events)
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-migration/02-02-SUMMARY.md` using the summary template. Record:
- Actual people count + role distribution (leadership/affiliate/staff)
- Actual event count (vs the "50+" estimate in REQUIREMENTS — reality as denominator per RESEARCH Open Question #3)
- Any events or people skipped (missing date, unresolved role, etc.) with reasons
- Role-resolution approach used (section-iteration vs. fallback) — note in the SUMMARY
- Handcrafted bio preservation decisions for bill-howe / chirag-shah / tanu-mitra post-diff review
</output>
